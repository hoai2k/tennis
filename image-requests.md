# Cursed Court — Image Requests (Pending)

No pending image requests right now. 🎉

All previously requested images were delivered, wired into the game, and
verified — see `image-requests-history.md` for the archive.

(The six Mech Mayhem character portraits in `public/portraits/` are
3D-rendered from the models via the charviewer portrait mode. They match the
existing portrait style, but if you'd like painted key-art versions instead,
just ask and entries can be added here.)

## How to add a new request
Add an entry below with: target **path** (under `public/`), **size**,
transparency yes/no, and a short **style prompt**. The game prefers assets
that drop into an existing `<img>` slot with a procedural fallback, so include
the intended screen/usage too.
